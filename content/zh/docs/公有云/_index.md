---
title: "公有云"
weight: 66
---


## 安全组

**aws安全组和阿里云安全组的区别：**
- AWS 安全组：默认情况下，所有入站流量都是**被拒绝**的。你添加的每一条规则都是在“开绿灯”。只要流量匹配了其中任何一条允许规则，就会被放行。如果没有匹配到任何规则，流量就会被丢弃。
- 阿里云安全组：采用的是类似防火墙的“优先级”逻辑（1-100）。它既有“允许”也有“拒绝”。如果一个流量同时匹配到允许和拒绝，则看谁的优先级更高（数字越小优先级越高）。